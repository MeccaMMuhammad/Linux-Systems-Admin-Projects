## ✅ Outcome
- CSR successfully created and signed by the CA
- Simulated secure certificate issuance

## 📚 Commands Reference

```bash
# Create CSR (SecPlusLLC)
openssl req -newkey rsa:2048 -keyout SecPlusLLC_privatekey.pem -out SecPlusLLC.csr

# Move CSR to CA directory
cp SecPlusLLC.csr /home/aciadmin/Module_1/CSR/CA

# Generate CA Key
openssl genpkey -algorithm RSA -out CA_privatekey.pem

# Sign CSR (simulate CA)
openssl req -newkey rsa:2048 -nodes -keyout CA_privatekey.pem -x509 -days 1999 -out SecPlusLLC.crt

# Move signed certificate back to SecPlusLLC
cp SecPlusLLC.crt /home/aciadmin/Module_1/CSR/SecPlusLLC
